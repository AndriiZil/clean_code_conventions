/**
 * If we need a function that will get the user's bank information,
 * then we must not use a name like getUserInfo or something like that.
 * We should use getUserBankInfo to be more specific.
 */
